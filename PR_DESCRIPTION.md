# 配色实验版：探索核心配色中可以沉淀为算法的部分

这次更新围绕核心配色模块展开：把已有配色经验中可以明确计算、重复执行的部分做成通用算法，验证它们能承担多少试排和比较工作，以及哪些判断仍需要交给 AI。新功能默认关闭，先作为独立实验版验证，暂不合并到稳定版。

## 选择与下载版本

| 版本 | 查看代码 | 下载 |
|---|---|---|
| 稳定版 | [main](https://github.com/sn246akeak/jewel-level-scatter-tuning/tree/main) | [下载稳定版 ZIP](https://github.com/sn246akeak/jewel-level-scatter-tuning/archive/refs/heads/main.zip) |
| 本 PR 实验版 | [实验分支](https://github.com/sn246akeak/jewel-level-scatter-tuning/tree/codex/profile-candidate-optimizer) | [下载实验版 ZIP](https://github.com/sn246akeak/jewel-level-scatter-tuning/archive/refs/heads/codex/profile-candidate-optimizer.zip) |

两个版本可以通过仓库页面的分支下拉菜单选择。本 PR 保持草稿、不合并，默认分支仍是 `main`。

## 为什么做这次实验

现有 Skill 已经整理了图片分类、配色规则和不同图类的优先顺序。接下来要探索的是：这些经验中，哪些已经足够明确，可以从文字要求进一步变成实际参与配色的算法？

例如，颜色数量是否正确、近色接触是否减少、零散色点是否增加，都可以计算；交换色块、调整区域内的排色，也有可以复用的操作方式。如果把这些部分沉淀下来，就有机会减少 AI 每张图重复推演和试排的工作，让已有经验更稳定地参与生成过程。

但配色同时涉及取舍：减少近色混淆，可能让边界变杂；色块更集中，也可能让表情不清楚。把这些取舍全部固定下来，会限制 AI 根据具体图片判断的空间。

所以，这次先验证一个具体分工：算法按照图类优先级尝试方案、计算变化、列出代价；AI 判断主体、表情和整体观感，决定这些变化是否值得采用。实验要回答的是这条分工边界是否有效，以及核心配色里还有哪些部分值得继续沉淀。

## 本轮先沉淀哪些部分

| 交给通用算法 | 仍由 AI 判断 |
|---|---|
| 检查颜色数量、格子范围和配色硬要求 | 图里哪些区域承担主体、表情或包含关系 |
| 按明确方式交换色块、尝试区域内的不同排色 | 哪些关键区域需要保留或重新设计 |
| 按当前图类的优先顺序比较可计算的指标 | 指标改善后，图片是否真的更清楚、更协调 |
| 记录每个方案的改善与退步，限制尝试次数 | 有得有失时是否接受，或者退回修改设计 |

这轮新增的是初稿之后的候选搜索和比较，继续复用已有的生成与验证能力。脸部识别、包含关系和整体美感尚未转成可靠算法。

## 这次具体怎么做

**先保留原来的初稿，再尝试变化。** 沿用现有方式生成初稿，然后尝试交换同样大小的色块、调整一个区域内的颜色分布，或交换少量局部格子。每次变化都检查颜色数量、原图占用的格子和不能同色重叠等要求；明确要求保留的区域不参与改动。

**不同图类，按各自的顺序比较。** 例如，需要优先处理近色混淆的图，先比较近色问题；需要优先处理高亮、高饱和冲突的图，先比较这些接触问题。现有分类和规则顺序保持不变，脚本依照它们判断候选方案的取舍。

**把改善和代价一起展示。** 减少了刺眼的颜色接触，却增加了零散色点，不能只汇报前一项。同一条规则里的不同要求有得有失时，脚本会明确标记为需要判断的取舍。其他规则中变差的地方也会列出来。

**把最后的视觉选择留给 AI。** 最多展示原稿和两个不同方案，放在一起比较。AI 检查脸部是否清楚、主体是否好认、轮廓和包含关系是否合理，再决定采用哪一张。如果选择的方案有指标退步，还必须说明为什么视觉上可以接受。原稿始终可以选；都不好时，就修改设计重新生成。

**限制尝试次数，到了就停。** 搜索有次数和时间限制，不会一直试排。没有合适的变化就保留原稿，不为了凑方案强行改图。

## 使用流程与停止位置

读取原图 → 计算特征、确定图类 → 生成初稿 → 尝试不同排色 → 展示对照 → AI 选择并完成视觉检查 → 通过验证后进入原有编辑和保存流程。

新增加的断点就在“展示对照”之后：候选出来不代表完成，机器推荐也不能直接进入编辑器。必须明确选定方案并通过检查，才能继续。方案重新生成后，旧的检查结果不能沿用。

每张图仍只需要 AI 编写设计要求和最终视觉检查两份文件，其余分析、候选、预览和报告由通用脚本生成。已有的 KStage 填充与保存操作没有改动。

## 目前验证到了哪一步

- 53 项自动测试及一次完整的离线填充交接模拟通过。
- 用 14 张历史图对照，关闭新功能时，生成结果全部与旧版一致。
- 9 张同时符合两种测试图类的图片中，7 张在更换主分类后得到不同的推荐结果，说明分类顺序确实参与了搜索。
- 14 张图中有 12 张在对应优先级下出现机器指标改善，但其中 11 张也有其他指标退步。因此，这个数字不能理解为“12 张图已经变得更好看”。

看过实际预览后，部分图片仍存在脸部不够清楚、边界颜色变杂等问题。目前历史测试只覆盖了五种主分类，也还没有验证实际能减少多少人工修改时间。下一步需要继续约束这些关键区域，并用更多图片和人工反馈判断效果。

## 为什么先保留为实验版

这版已经具备生成候选、比较取舍和暂停检查的完整流程，但视觉收益还需要继续验证。因此先放在独立分支，以草稿 PR 的方式保留，不合并到主分支。

当前远端稳定版早于本地用于测试的版本，因此这个分支还包含四次配套的基础维护提交，涉及通用流水线、KStage 操作校验和执行问题对照库。这些改动是实验版的基础，会随实验分支一起提交；远端稳定版不更新。

稳定版继续用于正常做图。实验版需要明确开启 `design.optimizer.enabled` 才会使用新流程；没有开启时，仍按原来的方式运行。
